---
title:
type: concept
updated: YYYY-MM-DD
sources: []     # 路径（项目内相对路径，项目外绝对路径）和/或 URL
see_also: []    # 相关 wiki 页面的相对路径
tags: []
---

# <标题>

<!--
新页面的 `type` 只能从八种规范类型中选择：`concept`、`decision`、`bug`、
`bugfix`、`open-question`、`source`、`reference`、`synthesis`。
当前模板默认使用 `concept`；按页面内容改成其中最匹配的一种。

正文结构由 SCHEMA.md 定义。概念页面的默认约定：

- 一段引言
- ## 关键声明（逐条列出，每条附引用）
- ## 开放问题
- ## 参见

其他规范类型遵循 SKILL.md 与 SCHEMA.md 中各自的约定。

质量规则：每条声明都能追溯到 `sources:` 中的条目。正文中
对模糊内容要注明不确定性。
-->
